import { colors } from './../../../core/constants/colors';
import styled from 'styled-components';

export const StyledInput = styled.input`
  padding: 0;
  margin: 0;
  border: none;
  outline: none;

  background-color: ${colors.helperBlue3};

  padding: 10px 25px;
  border-radius: 6px;
`;
